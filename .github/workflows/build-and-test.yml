name: Build and test

on:
  pull_request:
    branches: [ master, main, upstream, release/** ]

  push:
    branches: [ master, main, upstream, release/** ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
    with:
      timeout: 150